# Auth Feature

このディレクトリには認証機能に関連するコンポーネント、フック、ユーティリティが配置されます。

## 構成

- `components/` - 認証関連のUIコンポーネント（ログインフォームなど）
- `hooks/` - 認証状態を扱うカスタムフック
- `utils/` - Cognito連携のユーティリティ関数
- `types.ts` - 認証関連の型定義
